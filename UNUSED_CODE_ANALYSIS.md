# Анализ неиспользуемого кода

## 🗑️ Неиспользуемые компоненты

### 1. **MapViewSwitcher.jsx** (КРИТИЧНО)
- **Путь:** `src/components/MapViewSwitcher.jsx`
- **Статус:** Полностью НЕИСПОЛЬЗУЕТСЯ
- **Описание:** Компонент для переключения между 2D и 3D картами
- **Причина:** Функциональность была перенесена в App.jsx с встроенным переключением видов в заголовке
- **Размер:** ~130 строк кода
- **Действие:** ❌ Удалить

**Почему он неиспользуется:**
- Не импортируется ни в одном файле
- В App.jsx есть собственная реализация переключения видов (кнопки "map", "catalog", "countries", "mugs")
- Содержит закомментированный импорт и неактуальную архитектуру

---

## ⚠️ Дублирующиеся утилиты

Следующие функции определены в НЕСКОЛЬКИХ файлах и могут быть объединены в общий модуль:

### 2. **normalizeIso2()** (ДУБЛИРОВАНИЕ)
- **Определена в:**
  - `src/App.jsx` (строка 15)
  - `src/components/CatalogPage.jsx` (строка 5)
  - `src/components/FlatMap.jsx` (строка 3)
- **Использование:** ✅ Используется активно во всех трёх файлах
- **Рекомендация:** 🔧 Перенести в общую утилиту `src/lib/utils.js`

### 3. **formatDate()** (ДУБЛИРОВАНИЕ)
- **Определена в:**
  - `src/App.jsx` (строка 10)
  - `src/components/CatalogPage.jsx` (строка 9)
- **Использование:** ✅ Используется в обоих файлах
- **Рекомендация:** 🔧 Перенести в общую утилиту `src/lib/utils.js`

### 4. **extractCountryPayload()** (МОЖЕТ БЫТЬ ОПТИМИЗИРОВАНА)
- **Определена в:** `src/App.jsx` (строка 19)
- **Использование:** ✅ Используется 2 раза в App.jsx (строки 240, 246)
- **Статус:** Используется, но только в одном файле
- **Примечание:** Можно оставить в App.jsx или перенести в утилиты

---

## 🔄 Дублирующиеся типы мугов и переводы

### 5. **getUiText()** (ДУБЛИРОВАНИЕ ПЕРЕВОДОВ)
- **Определена в:** `src/components/CatalogPage.jsx` (строка 55)
- **Статус:** Локализованные строки определены в функции
- **Проблема:** Жёсткие строки вместо использования системы переводов
- **Рекомендация:** 🔧 Перенести в `src/i18n/translations.js`

### 6. **getCountryLabel()** и **getMugTypeLabel()**
- **Определены в:** `src/components/CatalogPage.jsx` (строки 93, 103)
- **Статус:** Специфичны для страницы каталога
- **Использование:** ✅ Используются в CatalogPage
- **Оценка:** Могут остаться локально, но рассмотреть перенос в utils

---

## 💾 Дополнительные вспомогательные функции в CatalogPage

### 7. **getLocalizedValue()** и **getSearchableValue()**
- **Определены в:** `src/components/CatalogPage.jsx` (строки 14, 28)
- **Статус:** Специфичны для фильтрации и поиска
- **Использование:** ✅ Активно используются в процессе фильтрации
- **Оценка:** ✅ Правильно размещены в компоненте

### 8. **tr()** функция переводов
- **Определена в:** `src/components/CatalogPage.jsx` (строка 42)
- **Проблема:** Дублирует функциональность из `useLanguage()` в контексте
- **Рекомендация:** 🔧 Использовать `useLanguage().t()` вместо локальной функции

---

## 📋 Итоговая сводка

| Код | Тип | Статус | Действие |
|-----|------|--------|---------|
| **MapViewSwitcher.jsx** | Компонент | ❌ НЕИСПОЛЬЗУЕТСЯ | Удалить |
| **normalizeIso2()** | Функция | ✅ Дублирование | Объединить в utils |
| **formatDate()** | Функция | ✅ Дублирование | Объединить в utils |
| **extractCountryPayload()** | Функция | ✅ Используется | Оставить или перенести |
| **getUiText()** | Функция | ✅ Дублирование | Перенести в translations.js |
| **tr()** | Функция | ✅ Дублирование | Заменить на useLanguage().t() |

---

## 🎯 Рекомендации по рефакторингу

### Приоритет 1: Удаление (КРИТИЧНО)
- Удалить `src/components/MapViewSwitcher.jsx` (неиспользуется)

### Приоритет 2: Объединение дублей (ВАЖНО)
1. Создать `src/lib/utils.js` с общими функциями:
   ```javascript
   export function normalizeIso2(value) { ... }
   export function formatDate(value) { ... }
   ```

2. Добавить переводы в `src/i18n/translations.js`

3. Заменить локальные `tr()` на `useLanguage().t()` в CatalogPage

### Приоритет 3: Оптимизация (ОПЦИОНАЛЬНО)
- Перенести `getCountryLabel()` и `getMugTypeLabel()` в utils для переиспользования

---

## 💡 Дополнительные примечания

- ✅ **Хорошая архитектура:** Используется React Context для языка, правильная организация компонентов
- ⚠️ **Проблемы с масштабированием:** Дублирование утилит затруднит будущее обслуживание
- 📦 **Размер проекта:** Код в целом чистый, нет значительного мёртвого кода кроме MapViewSwitcher

